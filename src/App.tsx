import { Box, Button, Card } from '@mui/material';

function App() {
  return (
    <Box sx={{ padding: 2 }}>
      <Button color="primary" variant="contained">
        + Create todo
      </Button>

      <Card>test</Card>
    </Box>
  );
}

export default App;
